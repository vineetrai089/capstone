apiVersion: v1
kind: Service
metadata:
  name: hello-app
  namespace:
  labels:
    app: hello-app
spec:
  selector:
    app: hello-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
